Cypress hover example

This example shows how to trigger the hover state using `mouseover` and true CSS hover state via [cypress-real-events](https://github.com/dmtrKovalenko/cypress-real-events) plugin
